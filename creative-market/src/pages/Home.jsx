import Hero from "../components/Hero";
import ScrollingText from "../components/ScrollingText";

const Home = () => {
  return (
    <main>
      <Hero />
      <ScrollingText />
    </main>
  );
};

export default Home;
